# Implement G2 Physical Definition

Create the branch from updated `develop`:

```bash
git switch develop
git pull origin develop
git switch -c feature/g2-physical-definition
```

Copy this package into the repository root.

Then:

```bash
git status
git add .
git commit -m "docs: add physical definition and dry prototype plan"
git push -u origin feature/g2-physical-definition
```

Open the PR with:

- base: `develop`
- compare: `feature/g2-physical-definition`

Suggested title:

`docs: add HS-001 physical definition and dry prototype plan`
